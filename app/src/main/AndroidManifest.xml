<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.roll.dundjinntome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dt_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dt_logo_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".start.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewCharacter.ViewCharacterActivity" />
        <activity android:name=".newCharacter.MakeCharacterActivity"></activity>
    </application>

</manifest>